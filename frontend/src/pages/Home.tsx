import { Container, Typography } from '@mui/material'

function Home() {
  return (
    <Container maxWidth="xs" sx={{ mt: 8, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
      <Typography variant="h4">EliteCode</Typography>
    </Container>
  )
}

export default Home
